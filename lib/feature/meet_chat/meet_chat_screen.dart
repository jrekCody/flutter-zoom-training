import 'package:flutter/material.dart';

class MeetChatScreen extends StatelessWidget {
  const MeetChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Meet & Chat'),
        ),
        body: Center(child: Text('Meet & Chat')));
  }
}
